services:
  neo4j:
    image: neo4j:latest
    container_name: neo4j-bjj
    ports:
      - "7474:7474"
      - "7687:7687"
    env_file:
      - .env
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data: